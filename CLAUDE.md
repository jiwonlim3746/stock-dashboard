- 모든 설명은 한국어로 한다
- 코드에는 한국어 주석을 충분히 단다
- 파일을 수정할 때는 먼저 "무엇을 왜 바꾸는지"를 한국어 한두 문장으로 설명한다
- 전문 용어를 쓸 때는 괄호에 쉬운 풀이를 덧붙인다

# 프로젝트 주요 결정사항 (5대 투자원칙 지표, principles.py)

대화가 압축(요약)되어도 맥락이 유지되도록, 5대 지표 기능을 만들며 사용자와 합의한
핵심 결정들을 정리한다. 세부 구현은 principles.py를 직접 읽어서 확인할 것 —
아래는 "왜 이렇게 했는지"에 대한 근거 중심 요약이다.

## 채점 방식 원칙

- 축은 총 5개: ①경영진 역량·도덕성, ②해자·확장성, ③밸류에이션, ④주주환원, ⑤미래비전·적응력.
  각 축은 0~100점이며, **5개 축을 합친 총점/종합점수는 만들지 않는다** (사용자가 명시적으로
  거부함 — 서로 성격이 다른 축을 하나의 숫자로 합치면 오히려 판단을 흐린다는 이유).
  레이더 차트로 축별 점수만 나란히 보여준다.
- 실제 데이터가 없는 항목은 절대 임의의 값을 지어내지 않고 "데이터 없음"으로 표시한다.
  네트워크/API 실패로 못 가져온 경우는 "데이터 없음 (조회 실패)"로 구분 표시한다
  (아래 "DART 호출 제약" 절 참고).
- 한 축을 구성하는 세부 항목 중 점수가 매겨진 항목이 2개 미만이면
  (`MIN_RELIABLE_ITEM_COUNT = 2`) 그 축은 "신뢰도 낮음"으로 표시되고 레이더 차트에서 제외된다.
  근거가 1개뿐인 축에 100점 같은 극단값이 나와도 그대로 믿으면 안 되기 때문.
- 축①(경영진)·축④(주주환원)은 세부 항목을 **단순 평균**한다 (`_make_axis_result`).
  축②(해자)·축③(밸류에이션)·축⑤(미래비전)은 항목별 중요도가 달라서 **가중 평균**한다
  (`_make_weighted_axis_result`) — 점수가 없는 항목은 제외하고 남은 항목 가중치 비율로
  재정규화한다.

## 축별 가중치 (사용자가 직접 지정)

- **② 해자·확장성** — weights = [0.15, 0.30, 0.15, 0.20, 0.20]
  - 영업이익률(peer 대비 상대수준) 15% / 영업이익률 5년 변동성 30% / 매출총이익률(peer 대비) 15%
    / 매출 3년 CAGR 20% / ROE 5년 평균 20%
  - "이익률 수준"은 절대 기준이 아니라 peer 대비 상대값으로만 보고 가중치도 낮게 뒀다
    (예: 유통업처럼 원래 영업이익률이 낮은 업종이 절대 기준 때문에 불리해지는 것을 피하려는 목적).
    대신 변동성(표준편차, 30%)과 매출 성장 추세(CAGR, 20%)에 더 큰 비중을 줬다 —
    가격결정력(해자)이 있으면 이익률이 안정적으로 유지된다고 보기 때문.
- **③ 밸류에이션** — weights = [0.15, 0.15, 0.10, 0.10, 0.50]
  - PER 절대수준 15% / PBR 절대수준 15% / PER peer 대비(참고용) 10% / PBR peer 대비(참고용) 10%
    / 3년 자기 PER 밴드 백분위 50%
  - **peer 비교 비중을 20%(10%+10%)로 낮춘 이유**: peer를 누구로 넣느냐에 따라 결론(고평가/저평가)이
    쉽게 뒤집힌다는 게 실제로 확인됐다 (예: 삼성전자에 peer 1개만 추가해도 평가가 반대로 바뀜).
    반면 "3년 자기 PER 밴드 백분위"는 이 종목 자신의 과거 3개 연말 PER과만 비교하므로 peer 선택에
    좌우되지 않아 가장 신뢰도가 높다고 판단, 50%로 가장 크게 배정했다.
    peer 비교 항목에는 항상 비교 종목 목록을 함께 표시하고, "peer를 누구로 고르느냐에 따라 결론이
    바뀔 수 있다"는 경고 문구를 조건 없이(값이 서로 어긋날 때만이 아니라 항상) 붙인다.
  - 시클리컬(경기 순환) 업종은 밸류에이션 해석에 별도 주의가 필요하다는 안내 문구를 추가로 붙인다.
- **⑤ 미래비전·적응력** — weights = [0.5, 0.25, 0.25]
  - R&D 비율(수준) 50% / R&D 비율 증감(추세) 25% / CAPEX(수준+3년 추세) 25%
  - 절대 수준에 가장 큰 비중을 두고, 증감·추세는 보조 지표로 삼는다. R&D/CAPEX 모두 매출 감소로
    비율이 왜곡(가짜로 올라 보이는 것)되지 않도록 방어 로직이 들어가 있다.

## DART 호출 제약과 peer_cache.json

- Streamlit Cloud처럼 해외 서버에서 국내 DART Open API를 호출하면 접속이 느려서
  `ConnectTimeout`으로 앱 전체가 죽는 문제가 있었다. 해결: timeout 10초→30초로 늘리고
  실패 시 최대 2회 재시도(`financials._dart_request`), 모든 외부 API 호출을 try/except로
  감싸서 실패해도 앱이 죽지 않고 그 항목만 "데이터 없음 (조회 실패)"로 표시되게 했다.
  하나라도 실패한 항목이 있으면 화면 상단에 "일부 데이터를 불러오지 못했습니다" 배너를 띄운다.
- 축②·③은 종목마다 같은 업종 peer(최대 3~5개)의 PER/PBR/영업이익률/매출총이익률을 함께
  조회하는데, 이걸 매번 실시간으로 하면 종목 하나 볼 때 DART 호출이 크게 늘어 타임아웃의
  주요 원인이 됐다. 그래서 **peer 값은 미리 계산해 `peer_cache.json`에 저장**하고, 앱은
  실행 중 그 파일을 읽기만 한다 (`principles.py`의 `_load_peer_cache`/`_get_peer_metrics`).
  캐시에 없는 티커는 실시간 조회로 자동 대체(live-fallback)된다.
  - 파일 구조: `{"generated_at": "<UTC ISO8601>", "KR": {"티커": {"per":.., "pbr":.., "gross_margin":.., "operating_margin":..}}, "US": {...}}`
  - PER/PBR은 주가에 따라 매일 바뀌는 값이라 캐시가 금방 낡아진다는 한계가 있다. 그래서
    화면의 peer 비교 항목마다 `[peer 기준일: YYYY-MM-DD]`로 캐시 생성일을 항상 같이 보여주고,
    7일(`PEER_CACHE_STALE_DAYS = 7`)이 지나면 "⚠️ peer 데이터가 오래됐습니다" 경고를 자동으로 붙인다
    (`_get_peer_cache_info`/`_peer_cache_suffix`).

## peer 캐시 갱신 방법

- 분기 실적 발표 후, 또는 `KR_SECTOR_PEERS`/`US_SECTOR_PEERS`의 peer 목록을 수정했을 때
  로컬 PC 터미널에서 직접 실행한다 (앱이 자동으로 갱신하지 않음):
  ```
  python build_peer_cache.py
  ```
- 실행하면 `peer_cache.json`이 새로 생성된다. 배포판(Streamlit Cloud)에 반영하려면
  그 파일을 `git commit`·`git push` 해야 한다 (사용자가 직접 커밋 시점을 판단하며,
  자동 커밋/자동 배포는 하지 않는다).
- yfinance 데이터 중 은행주(JPM/BAC/WFC 등)는 "영업이익" 계정 자체가 없을 수 있어
  `row["영업이익"]`처럼 바로 접근하면 `KeyError`가 난다. `build_peer_cache.py`와
  `principles.py` 모두 `.get()`으로 안전하게 접근하도록 고쳐져 있다 — 앞으로 peer를
  추가할 때도 이 패턴을 유지해야 한다.
